허깅페이스, LLM, transformer

conda install pytorch torchvision -c pytorch # 파이토치
pip install sympy==1.13.1 fsspec
pip install transformers # 트랜스포머스_트랜스포머 알고리즘 기반 모델을 사용할 때 필요
pip install sentencepiece # 구글이 만든 자연어 토크나이저
pip install hf_xet # 허깅페이스 hub에서 대용량 모델 파일을 빠르게 다운로드 도구
pip install pillow
pip install sacremoses

pip freeze > requirements.txt # 현재 환경 저장
notepad reauirements.txt
pip install -r requirements.txt # requirements.txt에 있는 라이브러리들 설치

허깅페이스 -> 기초 defaulte

transformers.pipeline(task = '교안참조', model ='모델명은허깅페이스에서찾기')
로 사용이 가능함

허깅페이스는 모델을 다운로드 받아서 활용하는 데 사용하기 때문에
크게 공부해서 내가 튜닝하는 둥의 행위는 얼마나 의미가 있을런지..
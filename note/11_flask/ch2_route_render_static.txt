ch2_route_render_static

# python -m pip install --upgrade pip
# pip install flask
from flask import Flask

app = Flask(__name__) # 앱 인스턴스 생성
@app.route("/") # @ : 데코레이터를 통해 요청가능한 url 등록
def handler_viewFunc() :
    return "<h1>Hello, World</h1>"

render => render_template를 사용한다.
사용할 웹페이지와 전달할 파라미터를 전달
=> html에서 {{파라미터}} 로 전달 가능
-> 잘 사용하려면 jinja2 문법을 알아야..

static은 이름 그대로 고정값으로
정적 웹페이지 구현용임
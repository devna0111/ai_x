ch03_ 실습형_DNN손글씨데이터
# 사진 파일 흑백 만들기
from PIL import Image
import numpy as np

# 이미지 열기
img = Image.open('your_image.jpg')

# 흑백(Grayscale)으로 변환
gray_img = img.convert('L')

# NumPy 배열로 변환
gray_array = np.array(gray_img)

print(gray_array.shape)  # (높이, 너비)

ch02_DeepLearning 심화_ tensorflow -v1_2

엔트로피 : 특정 분포 내에서 정보량의 기대값
Cross Entropy는 엔트로피와 달리 정보량의 기댓값을 계산할 때 확률 부분과 정보량 부분이 서로 다른 분포에서 비롯

learning_rate를 tensorflow_v2에서 사용하는 방법
model.compile(optimizer=tf.keras.optimizers.Adam(learning_rate=0.001),
              loss='binary_crossentropy',
              metrics=['accuracy'])

# 데이터셋 생성(확인)
# 데이터셋 전처리
	-> 스케일 조정 등
# 모델 생성(Dense, Input 설정)
# 모델 학습과정 설정(compile(loss, optimizer, metrics))
# 모델 학습
# 모델 학습 과정 체크
# 모델 평가
# 모델 사용
# 모델 저장

역전파 알고리즘 : 파라미터 값을 레이어의 역순으로 계산. 논문으로 발표된 효율적인 방법

사기꾼을 찾는 것은 1000명 중 1명이 사기꾼이면 전부 정상이라고 하여 99.9% 정확도를 달성 가능 => accu가 제일 중요한 케이스가 아님
=> 정밀도와 재현율을 체크하는 것이 중요
( 사기꾼이다 중 사기꾼이 몇명인가, 사기꾼이 아닌것과 사기꾼인 것을 맞춘 것의 비중 등)
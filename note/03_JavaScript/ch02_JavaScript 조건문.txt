ch02_조건문
** JavaScript에서 새로운 객체를 생성하려면 New 클래스이름()을 통해 이루어진다.

1. if(조건){
	참일때 실행할 블록
}else if(조건){
1조건이 거짓일 때 2 조건이 참이면 실행할 블록
}else{
모든 조건이 거짓일 때 실행할 블록
}

2. switch
=> switch(조건){
	case 조건의 결과1 : 출력할 결과 ; break;
	case 조건의 결과2 : 출력할 결과 ; break;
	case 조건의 결과3 : 출력할 결과 ; break;
	default : 출력할 결과;

6. 삼항연산자 : (조건식)? 참일때 : 거짓일때 ;
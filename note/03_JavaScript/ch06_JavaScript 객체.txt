ch06_JavaScript 객체
** 
[ ], { }, Array()... 심지어 null 까지도 객체로 구분한다.
그러나 null은 원시타입으로 따로 구분되며 객체활용은 불가능하다.
* 에러이나 수정하지 않음

알고있는 자료형을 제외하면 모두 객체이다.

가장 많이 사용할 json형태면 (파이썬 딕셔너리형)
속성 추가는 obj.속성추가할이름 = 추가할 값/일반함수표현식
속성 삭제는 delete( obj.삭제할속성이름 )

클래스가 없는 대신 하나의 객체를 생성하는 데 용이함
메소드를 추가할 때 속성을 추가하는 방식과 동일하게 활용 가능

파이썬에서는 __str__ 속성의 return값이 print함수로 나왔는데
자바스크립트에서는 toString 으로 가능함

이렇게 잘 만든 객체들을 배열에 넣으면 객체배열로 형성되는데
for문을 통해 각 객체에 접근하여 정리가 가능함.

=> 사람이 손으로 작성하다보면 분명히 오타가 나서 key값 접근이 불가능할 수도 있음.

=> 그럼 객체생성함수를 활용해서 더 용이하게 사용할 수 있음
발전과정..?
**객체 생성 함수 -> 생성자함수 -> class : 
생성자 함수 : 파이썬과는 다르게 클래스 속에서 정의하는 생성자 함수가 아니라 자바스크립트는 객체를 생성하는 함수임
=> 인자를 받아서 객체 {key : 인자, ... 함수 : functiond(){} ...} 를 return하는 함수만 만들면 끝.
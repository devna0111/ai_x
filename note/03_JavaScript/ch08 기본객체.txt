ch08 기본객체 

String, Array, Date, Math
str = 'abc' ; // 자동으로 String 객체 생성
arr = [1,2,3] ;// 자동으로 Array 객체 생성.... 등

내가 만들지 않고 자바스크립트에서 제공하는 기본 객체들
=> 기본제공 메소드가 많으니 쓰면서 익히는 게 중요하다.

## 정규표현식 => 일치하는가, 포함하는가
	=> 정규표현식.test(변수) : 변수가 정규표현식에 포함되냐?

	=> 변수.match(정규표현식) : 변수가 정규표현식과 정확히 일치하냐?

Date() 객체
1. get----() 함수 다수 함유. 의미 잘 읽고 테스트하고사용하기
2. Date().getTime() => 1970.01.01 로 부터의 millsec을 따옴.
	== Date().valueof() 도 동일한 값을 로드

** 만약 특정일자를 Date객체로 만든다면
	이상하게도 월 구분만은 idx로 처리해서 0을 기입해야 1월이됨..
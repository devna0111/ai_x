1. 데이터를 처음 받게 되면 info()를 체크하여 전체 데이터의 기본 정보를 체크한다. 
2. DataFrame.isnull().sum()을 활용하여 결측치를 체크한다.
3. 데이터의 수에 따라 결측치가 있는 데이터를 어떻게 활용할지 확인이 필요하다.
    1) missingno 라이브러리의 bar, matrix를 통해 결측치에 대한 내용을 시각화하여 확인한다. heatmap을 통해 corr도 체크 가능하다
    2) 데이터 분석에 결측치가 많아 불필요한 데이터를 drop시킨다.
4. 1차 가공된 데이터의 sample을 보고 1. 동일한 정보(ex. 행정동명, 법적동명 등)는 단일화하고 
    2. 현재 내가 활용하지 않을 데이터(ex)국가관리번호)는 drop하여 메모리를 확보한다.
5. 3번, 4번이 끝난 데이터는 우선 백업.

6. 그래프를 그려 데이터를 시각화한다.
    보고서의 목적에 따라 그래프의 선명도, 한글폰트의 활용 여부를 체크하여 환경설정을 우선 한다.
    조건에 따라 groupby절과 pivot_table을 통해 시각화에 필요한 데이터를 가공한다.
    xlim, xticks를 적절히 사용하여 그래프의 가독성을 높인다.
    